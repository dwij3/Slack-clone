import styles from './AddMessage.module.css';

const AddMessage = () => {

    return (
        <div className={styles.addMessage}>
            Add Message Section:
        </div>
    )
}

export default AddMessage;